```
                ┌─────────────────┐
                │   СУПЕРЮЗЕР     │
                │   superadmin    │
                └────────┬────────┘
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│    АДМИНЫ     │ │  МЕНЕДЖЕРЫ    │ │  БУХГАЛТЕРЫ   │
│    admins     │ │   managers    │ │  accountants  │
│  (полный      │ │  (почти всё,  │ │(фин. данные)  │
│   доступ)     │ │  кроме фин.)  │ │               │
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                 │                  │
        └─────────────────┼──────────────────┘
                          │
                ┌─────────▼─────────┐
                │    ПРОДАВЦЫ       │
                │      sales        │
                │ (UPDATE products, │
                │  SELECT suppliers)│
                └─────────┬─────────┘
                          │
                ┌─────────▼─────────┐
                │     КЛИЕНТЫ       │
                │    customers      │
                │ (SELECT products, │
                │  INSERT orders)   │
                └───────────────────┘
```

```sql

─── Сеть/Хост (pg_hba.conf)
   └──️ Аутентификация (md5, scram-sha-256, peer...)
       └──️ Роли / Пользователи / Группы
           └──️ Привилегии (GRANT)
               └──️ Row Level Security (RLS)
                   └──️ SSL/TLS шифрование
```

```sql
── Клиент (приложение) ──[Шифрование]──> Сервер PostgreSQL
     (Данные защищены от перехвата)
```

```
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Сервер                        │
├─────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────┐  │
│  │                    SSL/TLS (шифрование)               │  │
│  │  [Клиент] <───[Зашифрованный канал]───> [Сервер]      │  │
│  └───────────────────────────────────────────────────────┘  │
│                              │                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │    sslinfo (диагностика SSL)                          │  │
│  │    ┌─────────────────┐    ┌──────────────────────┐    │  │
│  │    │ssl_is_used()    │    │ssl_version()         │    │  │
│  │    │ssl_cipher()     │    │ssl_client_dn()       │    │  │
│  │    └─────────────────┘    └──────────────────────┘    │  │
│  └───────────────────────────────────────────────────────┘  │
│                              │                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │    sepgsql (мандатный контроль)                       │  │
│  │    ┌─────────────────┐    ┌──────────────────────┐    │  │
│  │    │Метка: секретно  │    │Метка: допуск 2       │    │  │
│  │    │Таблица X        │    │Пользователь Y        │    │  │
│  │    └─────────────────┘    └──────────────────────┘    │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```